# Métricas SC-2: Chips de Geolocalización

**Solicitud de Cambio:** SC-2 — La hacienda tiene la necesidad de conectar a las reses chips para la geolocalización.

---

## Métricas identificadas

| Métrica | Qué mide | Unidad | Cálculo/obtención | Relación con SC-2 | Evidencia en el código |
|---------|----------|--------|-------------------|-------------------|------------------------|
| Cantidad total de chips registrados | Número total de chips instalados que visualiza el sistema | Unidades (chips) | Conteo de filas en la tabla de chips de la vista Index. Cada fila representa un chip persistido en la tabla `chips`. Fuente: `_servicioChip.ListarChips()` → `SELECT * FROM chips` | Capacidad del sistema de tener chips registrados para geolocalización | `ChipController.cs:28` (`ListarChips()`), `Index.cshtml:43-89` (renderiza tabla con `@foreach (var chip in Model)`) |
| Cantidad de reses con chip instalado | Número de reses que muestra el sistema con chip asociado | Unidades (reses) | En la vista Instalar, cada opción de res muestra texto "YA TIENE CHIP" cuando `res.Chip != null`. Fuente: `_gestorReses.ListarReses()` filtra reses y evalúa propiedad `Chip` | Mide la cobertura del sistema de geolocalización sobre el ganado | `Instalar.cshtml:41` (`@(res.Chip != null ? "YA TIENE CHIP" : "Sin chip")`), `ServicioChip.cs:40-41` |
| Cantidad de reses sin chip disponible para instalar | Número de reses candidatas a instalación (sin chip) que muestra el selector | Unidades (reses) | En la vista Instalar, cada opción muestra texto "Sin chip" cuando `res.Chip == null`. Conteo de esas opciones. Fuente: `_gestorReses.ListarReses()` | Permite medir la cobertura pendiente del sistema de geolocalización | `Instalar.cshtml:41` (`@(res.Chip != null ? "YA TIENE CHIP" : "Sin chip")`), `ServicioChip.cs:40-41` |
| Cantidad de geolocalizaciones registradas por chip | Número de registros de ubicación GPS mostrados en el historial de un chip | Unidades (registros) | Conteo de filas en la tabla de la vista Historial. Fuente: `_servicioGeo.ObtenerHistorialChip(numeroSerie)` → `SELECT * FROM geolocalizaciones WHERE chip_id = @ChipId` | Permite medir la cantidad de datos de geolocalización generados por cada chip | `ChipController.cs:86` (`ObtenerHistorialChip`), `Historial.cshtml:26-49` (renderiza tabla con `@foreach (var ubi in historial)`) |
| Cantidad total de geolocalizaciones en el sistema | Número total de ubicaciones GPS mostradas en el panel "Últimas Ubicaciones" | Unidades (registros) | Conteo de elementos en el list-group de la vista Index (panel derecho). Fuente: `_servicioGeo.ObtenerUltimasUbicaciones(50)` → `SELECT * FROM geolocalizaciones ORDER BY fecha_hora DESC LIMIT 50` | Volumen total de datos de geolocalización acumulados visibles | `ChipController.cs:29` (`ObtenerUltimasUbicaciones(50)`), `Index.cshtml:106-121` (renderiza con `@foreach (var ubi in ubicaciones.Take(10))`) |
| Precisión de la geolocalización | Margen de error en metros de cada registro GPS mostrado | Metros (m) | Lectura directa de la columna "Precisión" en la tabla de Historial. Fuente: propiedad `PrecisionMetros` del objeto `Geolocalizacion`, campo `precision_metros` en tabla `geolocalizaciones` | Indica la confiabilidad/fidelidad de cada registro de ubicación | `Historial.cshtml:44-46` (`@(ubi.PrecisionMetros.HasValue ? $"{ubi.PrecisionMetros:F1} m" : "N/A")`), `Geolocalizacion.cs:12` |
| Fecha de instalación del chip | Fecha en que se instaló cada chip, mostrada en la tabla principal | Fecha (dd/MM/yyyy) | Lectura directa de la columna "Fecha Instalación" en la vista Index. Fuente: propiedad `FechaInstalacion` del objeto `Chip`, campo `fecha_instalacion` en tabla `chips` | Permite determinar la antigüedad de los chips instalados | `Index.cshtml:79` (`@chip.FechaInstalacion.ToString("dd/MM/yyyy")`), `Chip.cs:11` |
| Tiempo entre registros de ubicación de un chip | Intervalo temporal entre dos registros consecutivos de geolocalización mostrados en el historial | Horas / Minutos | Diferencia entre valores de la columna "Fecha/Hora" de filas consecutivas en la vista Historial. Fuente: propiedad `FechaHora` del objeto `Geolocalizacion` | Permite medir la frecuencia de actualización de la ubicación de cada chip | `Historial.cshtml:41` (`@ubi.FechaHora.ToString("dd/MM/yyyy HH:mm")`), `Geolocalizacion.cs:11` |